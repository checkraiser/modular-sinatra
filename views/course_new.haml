-if flash[:error]
  #error
    =flash[:error]
-if flash[:success]
  #success
    =flash[:success]
-else
  Create new course
%form(name="new_game" action="/courses/create" method="post")
  %label(for="name") Name:
  %input(type="text" name="name")
  %label(for="code") Code:
  %input(type="text" name="code")
  %input(type="submit" value="Start a New Course!")
